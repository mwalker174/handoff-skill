# NNN — <one-line title: what a stranger could resume from>

**Session date:** <source-of-truth date, named> . <If an external system disagrees: "System X
timestamps this session at <ts> — the two disagree; recorded rather than smoothed. Billing/audit
follows X. Needs a <who> call.">

Follows **<NNN-1>** (<one line on the prior doc>). <One sentence on what this session was: the
scientific half / the operational half / a correction pass.>

---

## 1. <Workstream>

<Claim, then the evidence that produced it: command run, file, id, line number, exit text.>
<Label material claims: verified by <command> / inferred / not exercised.>

## 2. Coordinates of anything created or changed outside this repo

| thing | identifier | how to verify | how to undo |
|---|---|---|---|
|  |  |  |  |

## 3. Resume here (paste-able)

```
<the exact command that re-queries live state>
```

## 4. What "good" looks like on the next check

| check | expected | what a mismatch means |
|---|---|---|
|  |  |  |

**Cost / time / size:** <measured value + source> — or **"not measured"**, plus where to read the
real figure. Never an estimate presented as a measurement.

## 5. Gotchas found (only ones actually hit, with the error text)

1. <Symptom: exact error string> → <cause> → <fix>. Mark whether the tool accepted it anyway
   (201-with-invalid-body, clean exit with no effect, silent symlink skip).

## 6. Corrections to earlier documents

- **Was:** <wrong claim> . **Now:** <right claim> . Evidence: <command/output>. Why it mattered:
  <decision or workstream the wrong claim had already shaped>.

## 7. Deliverables

| file | change |
|---|---|
|  |  |

**Commits / pushes:** <repo> → <sha>, remote head re-read and confirmed: <sha>.
**External mutations made:** <list, reversible vs not>.

## Open items / next steps

- [ ] <the single highest-value next action, stated as a verb + target + how to know it worked>
- [ ] <everything else unfinished, carried forward verbatim from the previous doc>
